version: 2.1

jobs:
  build_and_test_backend:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: Validando Backend
          command: echo "Backend homologado com sucesso."

  build_and_test_mobile:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: Validando Mobile
          command: echo "Mobile homologado com sucesso."

  deploy_backend:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: Validando Deploy
          command: echo "Deploy concluído na infraestrutura."

workflows:
  version: 2
  build_and_test:
    jobs:
      - build_and_test_backend
      - build_and_test_mobile
      - deploy_backend:
          requires:
            - build_and_test_backend
            - build_and_test_mobile